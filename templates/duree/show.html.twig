{% extends 'base.html.twig' %}

{% block title %}Duree{% endblock %}

{% block body %}
    <h1>Duree</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ duree.id }}</td>
            </tr>
            <tr>
                <th>Nbr_de_jour</th>
                <td>{{ duree.nbrDeJour }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ duree.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_duree_index') }}">back to list</a>

    <a href="{{ path('app_duree_edit', {'id': duree.id}) }}">edit</a>

    {{ include('duree/_delete_form.html.twig') }}
{% endblock %}
