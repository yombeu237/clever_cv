{% extends 'base.html.twig' %}

{% block title %}Souscrire{% endblock %}

{% block body %}
    <h1>Souscrire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ souscrire.id }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ souscrire.dateDebut ? souscrire.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ souscrire.dateFin ? souscrire.dateFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_souscrire_index') }}">back to list</a>

    <a href="{{ path('app_souscrire_edit', {'id': souscrire.id}) }}">edit</a>

    {{ include('souscrire/_delete_form.html.twig') }}
{% endblock %}
