<form method="post" action="{{ path('app_souscrire_delete', {'id': souscrire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ souscrire.id) }}">
    <button class="btn">Delete</button>
</form>
