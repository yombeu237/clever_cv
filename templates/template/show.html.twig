{% extends 'base.html.twig' %}

{% block title %}Template{% endblock %}

{% block body %}
    <h1>Template</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ template.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ template.image }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ template.type }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ template.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_template_index') }}">back to list</a>

    <a href="{{ path('app_template_edit', {'id': template.id}) }}">edit</a>

    {{ include('template/_delete_form.html.twig') }}
{% endblock %}
