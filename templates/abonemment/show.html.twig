{% extends 'base.html.twig' %}

{% block title %}Abonemment{% endblock %}

{% block body %}
    <h1>Abonemment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonemment.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ abonemment.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ abonemment.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_abonemment_index') }}">back to list</a>

    <a href="{{ path('app_abonemment_edit', {'id': abonemment.id}) }}">edit</a>

    {{ include('abonemment/_delete_form.html.twig') }}
{% endblock %}
