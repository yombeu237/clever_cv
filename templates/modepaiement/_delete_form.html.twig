<form method="post" action="{{ path('app_modepaiement_delete', {'id': mode_de_paiement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_de_paiement.id) }}">
    <button class="btn">Delete</button>
</form>
