{% extends 'base.html.twig' %}

{% block title %}ModeDePaiement{% endblock %}

{% block body %}
    <h1>ModeDePaiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_de_paiement.id }}</td>
            </tr>
            <tr>
                <th>Mode</th>
                <td>{{ mode_de_paiement.mode }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ mode_de_paiement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_modepaiement_index') }}">back to list</a>

    <a href="{{ path('app_modepaiement_edit', {'id': mode_de_paiement.id}) }}">edit</a>

    {{ include('modepaiement/_delete_form.html.twig') }}
{% endblock %}
