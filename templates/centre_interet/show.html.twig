{% extends 'base.html.twig' %}

{% block title %}CentreInteret{% endblock %}

{% block body %}
    <h1>CentreInteret</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centre_interet.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ centre_interet.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_centre_interet_index') }}">back to list</a>

    <a href="{{ path('app_centre_interet_edit', {'id': centre_interet.id}) }}">edit</a>

    {{ include('centre_interet/_delete_form.html.twig') }}
{% endblock %}
