<form method="post" action="{{ path('app_connaissance_delete', {'id': connaissance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ connaissance.id) }}">
    <button class="btn">Delete</button>
</form>
